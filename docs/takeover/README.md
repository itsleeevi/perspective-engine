# How They Took Over — shipped videos

Newest first. Originality QA reads this index (not `docs/videos/` and not `docs/business/`). Each page needs a **Do not copy** section so the next title cannot steal its hook, rise structure, turning points, flywheel, or ending.

- [How Nvidia Took Over AI](nvidia-took-over-ai.md) — two consumer boards / second language / empty racks, Kokoro `am_michael` 1.15 (filings through Q1 FY2027; no “today is DATE” in the VO)

When a cut ships, add it here and write `docs/takeover/<slug>.md`.
