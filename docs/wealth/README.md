# Quiet Wealth — shipped videos

Newest first. Originality QA reads this index (not `docs/videos/`, not `docs/business/`, not `docs/takeover/`). Each page needs a **Do not copy** section so the next title cannot steal its opening object, supporting cast, or ending.

When a cut ships, add it here and write `docs/wealth/<slug>.md`.
